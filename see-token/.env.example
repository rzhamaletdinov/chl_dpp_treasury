# Copy this file to `.env` and fill in real values before running `npm run deploy:*`.
# `.env` is git-ignored.
#
# IMPORTANT: before any deploy is even possible, set the SEE_MULTISIG owner
# address inside contracts/SEE.sol (search for `TODO(see)`). Until that constant
# is non-zero, `initialize` reverts with "Ownable: new owner is the zero address".
# Also set the final SEE display name there (the `__SeeToken_init` call) — the
# EIP-712 permit domain binds to it.

# --- RPC ---
# Optional. If unset, defaults from hardhat.config.js are used (public RPC).
BSC_RPC_URL=https://bsc-dataseed.binance.org
BSC_TESTNET_RPC_URL=https://data-seed-prebsc-1-s1.binance.org:8545

# --- Deployer ---
# Hex private key WITHOUT the 0x prefix is also accepted by Hardhat,
# but with 0x is fine too. NEVER commit a real key.
PRIVATE_KEY=

# --- Contract verification (optional) ---
BSCSCAN_API_KEY=

# Note: SEE.initialize() takes no arguments — name/symbol are hardcoded in
# contracts/SEE.sol, so there are no initialize-arg env vars here.
